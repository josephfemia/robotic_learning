import "./storage.js"; // install window.storage before anything reads progress
import { createApp } from "vue";
import App from "./App.vue";
import "./assets/styles.css";

createApp(App).mount("#app");
